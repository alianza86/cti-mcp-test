[project]
name = "mcp-library-example"
version = "0.1.0"
description = "MCP de ejemplo (solo lectura) para una biblioteca de libros sobre Postgres"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
    "psycopg[binary]>=3.1",
]
